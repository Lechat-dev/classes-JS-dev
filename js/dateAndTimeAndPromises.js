// const init = new Date();
// const result = {};

// const id = setInterval(() => {
//   if (Object.keys(result).length === 4) {
//     clearInterval(id);
//   }

//   const current = new Date();
//   result[`time_${Math.round((current - init) / 2000)}`] = current - init;
//   console.log(result);
// }, 2000);

// setTimeout(() => {
//   console.log(
//     Object.values(result)[Object.values(result).length - 1] /
//       Object.values(result).length,
//   );
// }, 2000);

